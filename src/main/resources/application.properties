# ============================================
# DATABASE CONFIGURATION
# ============================================
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://dpg-d78c278gjchc73erk26g-a:5432/placement_portal_okd1}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:placement_portal_okd1_user}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:r7biCfSdr0rEKB8bufE9mgNTqFXiae1r}
spring.datasource.driver-class-name=org.postgresql.Driver

# ============================================
# JPA / HIBERNATE CONFIGURATION
# ============================================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# ============================================
# SERVER CONFIGURATION
# ============================================
server.port=${PORT:8080}

# ============================================
# GOOGLE OAUTH2 CLIENT CONFIGURATION
# ============================================
google.oauth.client-id=${GOOGLE_OAUTH_CLIENT_ID:}

# ============================================
# JWT (Security Token) Configuration
# ============================================
jwt.secret=${JWT_SECRET:placementPortalSecretKey}
jwt.expiration=86400000

# ============================================
# LOGGING
# ============================================
logging.level.org.springframework.security=INFO
logging.level.org.hibernate.SQL=INFO